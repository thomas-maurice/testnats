// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: lua/v1/lua.proto

package luav1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/thomas-maurice/testnats/gen/lua/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// LuaServiceName is the fully-qualified name of the LuaService service.
	LuaServiceName = "lua.v1.LuaService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// LuaServiceExecuteProcedure is the fully-qualified name of the LuaService's Execute RPC.
	LuaServiceExecuteProcedure = "/lua.v1.LuaService/Execute"
)

// LuaServiceClient is a client for the lua.v1.LuaService service.
type LuaServiceClient interface {
	// Execute runs a Lua script and returns the result.
	Execute(context.Context, *connect.Request[v1.ExecuteRequest]) (*connect.Response[v1.ExecuteResponse], error)
}

// NewLuaServiceClient constructs a client for the lua.v1.LuaService service. By default, it uses
// the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewLuaServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) LuaServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	luaServiceMethods := v1.File_lua_v1_lua_proto.Services().ByName("LuaService").Methods()
	return &luaServiceClient{
		execute: connect.NewClient[v1.ExecuteRequest, v1.ExecuteResponse](
			httpClient,
			baseURL+LuaServiceExecuteProcedure,
			connect.WithSchema(luaServiceMethods.ByName("Execute")),
			connect.WithClientOptions(opts...),
		),
	}
}

// luaServiceClient implements LuaServiceClient.
type luaServiceClient struct {
	execute *connect.Client[v1.ExecuteRequest, v1.ExecuteResponse]
}

// Execute calls lua.v1.LuaService.Execute.
func (c *luaServiceClient) Execute(ctx context.Context, req *connect.Request[v1.ExecuteRequest]) (*connect.Response[v1.ExecuteResponse], error) {
	return c.execute.CallUnary(ctx, req)
}

// LuaServiceHandler is an implementation of the lua.v1.LuaService service.
type LuaServiceHandler interface {
	// Execute runs a Lua script and returns the result.
	Execute(context.Context, *connect.Request[v1.ExecuteRequest]) (*connect.Response[v1.ExecuteResponse], error)
}

// NewLuaServiceHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewLuaServiceHandler(svc LuaServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	luaServiceMethods := v1.File_lua_v1_lua_proto.Services().ByName("LuaService").Methods()
	luaServiceExecuteHandler := connect.NewUnaryHandler(
		LuaServiceExecuteProcedure,
		svc.Execute,
		connect.WithSchema(luaServiceMethods.ByName("Execute")),
		connect.WithHandlerOptions(opts...),
	)
	return "/lua.v1.LuaService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case LuaServiceExecuteProcedure:
			luaServiceExecuteHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedLuaServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedLuaServiceHandler struct{}

func (UnimplementedLuaServiceHandler) Execute(context.Context, *connect.Request[v1.ExecuteRequest]) (*connect.Response[v1.ExecuteResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("lua.v1.LuaService.Execute is not implemented"))
}
